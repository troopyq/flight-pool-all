import { checkUser } from './func/functions.js';

checkUser('signin');

// const state = {
//   token: document.cookie,
// };

// let reg = /token=\w+/;
// let str = state.token.match(reg);
// const token = str[0].replace(/token=/, '');
// console.log('token: ' + token);

// export default state;
